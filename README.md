# 🐾 AmitVet — מערכת לניהול מרפאה וטרינרית

אתר לניהול משימות ותקשורת בין לקוחות לווטרינר הראשי. לקוחות רשומים יכולים
לשלוח פניות, לקבוע פגישות ולנהל את החיות שלהם; הווטרינר מנהל הכל מלוח בקרה אחד.

הממשק כולו בעברית עם תמיכה מלאה ב-RTL.

## יכולות

**ללקוח:**
- הרשמה והתחברות
- ניהול חיות מחמד (סוג, גזע, מין, גיל, משקל, הערות)
- צפייה בהיסטוריה רפואית, חיסונים, מרשמים ומעקב משקל של החיה
- בקשת תורים (בדיקה, חיסון, ניתוח, חירום ועוד) וביטולם
- פתיחת פניות לווטרינר וצ'אט דו-כיווני בשרשור (בזמן אמת)
- פאנל תזכורות: תורים קרובים וחיסונים שמועדם מתקרב
- צפייה בפרטי המרפאה ושעות הפעילות

**לווטרינר הראשי:**
- לוח בקרה: בקשות פגישה ממתינות, פניות פתוחות, משימות, תורי היום, חיסונים מתקרבים
- אישור / דחייה / קביעה-מחדש של פגישות והוספת הערות
- מענה לפניות וניהול סטטוס (פתוח / בטיפול / נסגר) ודחיפות
- ניהול רשומות רפואיות, חיסונים, **מרשמים/תרופות** ו**מעקב משקל** (עם גרף) לכל חיה
- **לוח משימות** פנימי (מטלות, מעקבים, התקשרויות) עם דחיפות ויעד
- **פרטי מרפאה ושעות פעילות** שהלקוחות רואים
- רשימת כל הלקוחות והמטופלים, עם תגי התראה בתפריט

## 🚀 העלאה לאוויר — GitHub Pages + Supabase (מומלץ)

ארכיטקטורה זו נותנת אתר שנטען **מיד** (GitHub Pages) עם שרת **תמיד-ער** וחינמי
(Supabase) שמטפל בהתחברות, בשמירת הנתונים ובהודעות בזמן אמת. הממשק נמצא בתיקיית
`docs/`.

### שלב 1 — הקמת Supabase

1. היכנסו ל-[supabase.com](https://supabase.com), צרו חשבון ולחצו **New project**
   (בחרו אזור קרוב, למשל Frankfurt). שמרו את סיסמת מסד הנתונים.
2. בתפריט הצד: **SQL Editor → New query**. העתיקו את כל התוכן של
   הקובץ [`supabase/schema.sql`](supabase/schema.sql), הדביקו, ולחצו **Run**.
   זה יוצר את כל הטבלאות וכללי האבטחה (כולל התכונות הנוספות).

   > 🔄 **שדרגתם מגרסה קודמת?** אם כבר הרצתם בעבר גרסה ישנה של `schema.sql`,
   > הריצו עכשיו את [`supabase/migration_v2.sql`](supabase/migration_v2.sql)
   > כדי להוסיף את הטבלאות החדשות (משימות, מרשמים, משקל, פרטי מרפאה). בטוח להריץ.
3. **Authentication → Providers → Email**: ודאו ש-Email מופעל, וכבו את
   **"Confirm email"** (כדי שההרשמה תכניס מיד בלי אימות מייל).

### שלב 2 — חיבור הממשק

ב-Supabase לכו אל **Settings → API** והעתיקו שני ערכים אל הקובץ `docs/config.js`:

```js
window.AMITVET_CONFIG = {
  SUPABASE_URL: "https://xxxx.supabase.co",   // Project URL
  SUPABASE_ANON_KEY: "eyJhbGci...",            // anon public key
};
```

> ה-anon key מיועד לצד-לקוח ובטוח לפרסום — האבטחה נאכפת ע"י כללי ה-RLS שב-SQL.

### שלב 3 — פרסום ב-GitHub Pages

1. דחפו את הקוד ל-GitHub.
2. ב-repo: **Settings → Pages**.
3. תחת **Source** בחרו **Deploy from a branch**, ובחרו את הברנץ' שלכם
   ואת התיקייה **`/docs`**. שמרו.
4. כעבור דקה תקבלו כתובת כמו `https://USERNAME.github.io/AmitVet/`.

### שלב 4 — הגדרת הווטרינר הראשי

היכנסו לאתר ו**הירשמו** עם המייל שלכם. כדי להפוך את החשבון לווטרינר, חזרו ל-Supabase
**SQL Editor** והריצו:

```sql
update public.profiles set role = 'vet' where email = 'האימייל-שלכם@דוגמה.com';
```

רעננו את האתר — עכשיו תראו את לוח הבקרה של הווטרינר. כל שאר הנרשמים הם לקוחות.

---

## הרצה מקומית / אירוח Node (אופציונלי)

הפרויקט כולל גם שרת Node.js עצמאי (תיקיות `server/` ו-`public/`) עם SQLite מובנה —
שימושי לפיתוח מקומי או לאירוח עצמאי ללא Supabase:

```bash
npm install
npm start         # http://localhost:3000
```

בגרסה זו נוצרים חשבונות דמו: וטרינר `admin@amitvet.local` / `admin1234`,
לקוח `dana@example.com` / `client1234`. ניתן לפרוס אותה ב-Render / Fly.io / Docker
(ראו `Dockerfile` ו-`render.yaml`). ⚠️ שירותי Node חינמיים "נרדמים" אחרי חוסר
פעילות — לכן לאתר חי ומהיר עדיף נתיב GitHub Pages + Supabase שלמעלה.

## מבנה טכני

הפרויקט תומך בשתי דרכי הרצה, חולקות את אותו ממשק וסכמת נתונים:

- **GitHub Pages + Supabase** (מומלץ): ממשק סטטי ב-`docs/` המדבר מול Supabase
  (PostgreSQL + Auth + Realtime). אבטחה ע"י Row Level Security.
- **שרת Node עצמאי:** Express + SQLite מובנה (`node:sqlite`), אימות JWT בעוגיית
  httpOnly עם הצפנת bcrypt.

הממשק: HTML/CSS/JS טהור ב-RTL, ללא שלב build.

```
docs/               גרסת GitHub Pages
  index.html        טוען את supabase-js + config + app
  config.js         פרטי חיבור ל-Supabase (יש למלא)
  app.js            ממשק + שכבת נתונים מול Supabase
  styles.css
supabase/
  schema.sql        טבלאות + טריגרים + מדיניות RLS (הקמה מאפס)
  migration_v2.sql  דלתא לתכונות הנוספות (להקמות קיימות)
server/             שרת Node עצמאי (אופציונלי)
  index.js, db.js, auth.js, routes/
public/             ממשק לגרסת ה-Node
```

## משתני סביבה

| משתנה        | ברירת מחדל | תיאור                          |
|--------------|------------|--------------------------------|
| `PORT`       | 3000       | פורט השרת                      |
| `JWT_SECRET` | נוצר אוטומטית | מפתח לחתימת טוקנים (מומלץ בפרודקשן) |
| `NODE_ENV`   | —          | `production` מפעיל עוגיות secure |
| `DATA_DIR`   | `./data`   | תיקיית מסד הנתונים (כוונו לדיסק קבוע באירוח) |

## עיצוב — שפת "Garden"

הממשק מעוצב בשפת העיצוב **"Garden"** של מערכת ה-design system של FlyLab Pro:
קנבס קרם חמים, אקסנט ירוק-מרווה (`#5a8a5e`), פסטלים רכים, פינות מעוגלות נדיבות
וצללים חמים ושכבתיים. כותרות בגופן serif (Spectral, ולעברית Frank Ruhl Libre)
מעל גוף בגופן grotesk (Plus Jakarta Sans, ולעברית Heebo). האייקונים הם
**Lucide** (קווי, באקסנט המרווה) והמותג בצבע אלמוג (`#f2785c`).

> התאמת עברית: Spectral ו-Plus Jakarta Sans אינם כוללים אותיות עבריות, לכן
> נוספו Frank Ruhl Libre (serif עברי) ו-Heebo (grotesk עברי) כדי לשמר את
> כוונת העיצוב גם ב-RTL. כל הטוקנים מוגדרים ב-`docs/styles.css`.

## רעיונות להמשך

- תזכורות אוטומטיות ב-SMS/אימייל לפגישות וחיסונים
- העלאת קבצים/תמונות (תוצאות בדיקות, צילומים)
- חיוב ותשלומים אונליין
- ניהול מספר וטרינרים וצוות
